{
  "rules": {
    "authSessions": {
      "$sessionId": {
        ".read": "auth != null",
        ".write": "auth != null && (!data.exists() || data.child('requesterId').val() === auth.uid || data.child('status').val() === 'pending')"
      }
    }
  }
}
