{
  "name": "kunji-rp-widget",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=iife --platform=browser --minify --legal-comments=none --outfile=../landing/rp.js && cp ../landing/rp.js ../landing/rp.v1.js && esbuild src/agent-demo.js --bundle --format=iife --platform=browser --minify --legal-comments=none --outfile=../landing/kunji-agent-demo.js"
  },
  "dependencies": {
    "@noble/curves": "^2.0.1",
    "qr-code-styling": "^1.9.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
