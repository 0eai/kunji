# The demo reads its session docs from the same Firebase project the Functions write to.
# Copy these from the kunji app's .env (the kunji-cc web config).
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
